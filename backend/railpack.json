{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "steps": {
    "install": {
      "inputs": ["...", { "local": true, "include": ["constraints.txt"] }]
    }
  },
  "deploy": {
    "startCommand": "uvicorn app.main:asgi --host 0.0.0.0 --port $PORT"
  }
}
